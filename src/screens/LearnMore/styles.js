import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  general: {
    fontFamily: 'System',
    fontSize: 15,
  },
  questions: {
    fontWeight: 'bold',
    fontFamily: 'System',
    fontSize: 15,
  },
  listView: {
    flex: 1,
    justifyContent: 'center',
    left: 1,
    backgroundColor: '#fff',
  },
  header: {
    position: 'absolute',
    justifyContent: 'center',
    alignItems: 'center',
  }
});